// navbar.js — kept for backwards-compat imports.
// HTML injection is now synchronous via navbar-inline.js (loaded in <body>).
// This module only pulls in the auth state watcher.
import './navbar-auth.js';
